import 'package:flutter/material.dart';

var drawer = const Drawer(
  backgroundColor: Colors.white,
  child: Column(
    children: [
      ListTile(title: Text("demo 1")),
      ListTile(title: Text("demo 1")),
      ListTile(title: Text("demo 1")),
      ListTile(title: Text("demo 1")),
    ],
  ),
);

var appBar = AppBar(backgroundColor: Colors.indigoAccent);
